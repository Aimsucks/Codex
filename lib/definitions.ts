export type UserPermissionsType = {
    isAdmin: boolean;
    isCurrentPluginEditor: boolean;
};

export type ItemViewerType = {
    categoryId?: number;
    parentCategoryId?: number;
    presetId?: number;
};
